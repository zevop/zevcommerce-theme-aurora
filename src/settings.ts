import { defineSettings } from '@zevcommerce/theme-sdk';

// Aurora uses the same settings schema as Prime
// The differences are in the default preset values, not the schema structure
export { settingsSchema } from '@zevcommerce/theme-prime';
